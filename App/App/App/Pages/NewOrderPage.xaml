<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="App.Pages.NewOrderPage"
             BackgroundColor="{StaticResource BackgroundColor}"
             Title="Nuevo Pedido"
             BindingContext="{Binding Main, Source={StaticResource Locator}}">>
  <Label Text="{Binding MainText}" VerticalOptions="Center" HorizontalOptions="Center" />
  <ScrollView>
    <StackLayout Padding="8">
      <Label TextColor="{StaticResource FontColor}"
             Text="Titulo"></Label>
      
      <Entry TextColor="{StaticResource FontColor}"
             BackgroundColor="White"></Entry>

      <Label TextColor="{StaticResource FontColor}"
                   Text="Descripcion"></Label>
      
      <Entry TextColor="{StaticResource FontColor}"
             BackgroundColor="White"></Entry>
      
      <Label TextColor="{StaticResource FontColor}"
             Text="Fecha Entrega"></Label>
      <DatePicker
        BackgroundColor="White"></DatePicker>
      <Button
        Style="{StaticResource GeneralButton}"
        Text="Guardar"></Button>
  
      
    </StackLayout>
  </ScrollView>
</ContentPage>